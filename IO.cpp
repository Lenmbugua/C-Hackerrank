#include <iostream>
using namespace std;
int main()
{
    int a, b, c;
    cin >> a >> b >> c ; //Reading three space separated integers from cin
    int sum = a + b + c; //calculating the sum
    cout << sum << endl;
    return 0;
}